# Task 1 구현 보고서 — Four-Anchor Semi-Realistic Style Contract

## 결과

`DONE_WITH_CONCERNS` — Task 1의 지정 문서·이미지 이동·정적 검증은 완료했다. 이동 후 보관본의 사전 SHA-256 값이 초기 콘솔 표에서 잘려 다시 전사한 값이 한 번 불일치했으나, 이동 직후 원본 대비 검증은 통과했고 실제 보관본 SHA-256을 다시 읽어 최종 검증을 통과했다. 파일 내용이나 경로를 되돌리거나 재이동한 일은 없다.

## 변경 파일

| 구분 | 경로 | 변경 내용 |
| --- | --- | --- |
| 신규 | `1. 프롬프트/1. 이미지 생성 모듈/라이브러리/스타일/정식 스타일 호출 규칙.md` | 세 가지 상호 배타적 그림체 적용 방식, 두 정식 스타일의 활성 앵커 선언, 제공 이미지 보존·기능성 예외, 실제 픽셀 하드 게이트, 앵커 콘텐츠 복제 금지, 참조 예산과 요구사항 확인서 기록 계약을 단일 정본으로 작성했다. |
| 수정 | `1. 프롬프트/1. 이미지 생성 모듈/라이브러리/스타일/붓터치 반실사.md` | 네 활성 앵커와 역할을 소유하도록 전환하고, 조합·참조 예산을 중앙 호출 규칙에 위임했다. 지정된 고정 영어 그림체 블록으로 교체했다. |
| 수정 | `1. 프롬프트/1. 이미지 생성 모듈/라이브러리/스타일/애니 페인팅.md` | 현재 세 활성 앵커 파일과 역할은 유지하고, 앵커 선택·참조 예산을 중앙 호출 규칙에 위임했다. |
| 수정 | `1. 프롬프트/1. 이미지 생성 모듈/라이브러리/스타일/공통 붓터치 마감 규칙.md` | 지정된 저수분 고정 영어 마감 블록으로 교체하고, 금지 항목을 희석된 워시·습윤 번짐·고인 경계·탈색 안개·거친 임파스토·CGI 광택·균일 텍스처 오버레이로 한정했다. |

## 이미지 이동 전후와 SHA-256

이동 전에는 모듈·후보·보관·활성 디렉터리의 절대 경로가 모두 이미지 생성 모듈 아래에 있는지 확인했다. 두 원본이 존재하고 두 목적지가 부재한 상태에서만 이동했다.

| 대상 | 이동 전 절대 경로 | 이동 후 절대 경로 | SHA-256 |
| --- | --- | --- | --- |
| 승인 교정본 | `G:\내 드라이브\TRPG 데이터\1. 프롬프트\1. 이미지 생성 모듈\라이브러리\견본\붓터치 반실사\후보\JRPG 양식화\04. 마감 앵커 교정본.png` | `G:\내 드라이브\TRPG 데이터\1. 프롬프트\1. 이미지 생성 모듈\라이브러리\견본\붓터치 반실사\04. 마감 앵커.png` | `4DD50806C3905725D60AB07AB72CEBE463D3A13496A03A3600D4DF44B0A080E7` |
| 기존 후보 | `G:\내 드라이브\TRPG 데이터\1. 프롬프트\1. 이미지 생성 모듈\라이브러리\견본\붓터치 반실사\후보\JRPG 양식화\04. 마감 앵커.png` | `G:\내 드라이브\TRPG 데이터\1. 프롬프트\1. 이미지 생성 모듈\라이브러리\견본\보관\붓터치 반실사 재구축 후보\04. 마감 앵커.png` | `82DF15CEFE6F685D96412501FCBDAFFE9A13C729672628C39D30ACD8B64E0289` |

이동 직후 승인 교정본과 활성 `04. 마감 앵커.png`, 기존 후보와 보관본의 SHA-256 동등성 검사를 각각 통과했다. 애니 페인팅의 기존 세 활성 앵커는 이름 변경·교체·이동하지 않았다.

## 실행한 검증과 실제 결과

| 검증 명령·검사 | 실제 결과 |
| --- | --- |
| Task 1 Step 1의 `Resolve-Path`, 존재 여부, 목적지 부재, SHA-256 확인 PowerShell | 통과. 두 원본 존재, 두 목적지 부재, 모든 해석 경로가 이미지 모듈 내부였다. |
| Task 1 Step 2의 두 `Move-Item`과 이동 후 SHA-256 동등성 검사 | 통과. 예외 없음. |
| Task 1 Step 6의 `Get-ChildItem`, 네 파일명 `rg`, 호출 규칙·참조 예산·보존·기능성 예외 `rg`, 두 새 영어 블록 시작문 `rg` | 통과. 활성 반실사 폴더 최상위 PNG는 `01. 핵심 얼굴 앵커.png`, `02. 나이 표현 앵커.png`, `03. 전신 앵커.png`, `04. 마감 앵커.png`의 정확히 네 개였다. 중앙 호출 문구와 각 정본 영어 블록 시작문을 찾았다. |
| 활성 PNG 이름·개수의 `Compare-Object` 검사 | 통과. 예상 네 파일과 정확히 일치했다. |
| 활성·보관 04 SHA-256 재검증 | 통과. 활성 `4DD50806C3905725D60AB07AB72CEBE463D3A13496A03A3600D4DF44B0A080E7`, 보관 `82DF15CEFE6F685D96412501FCBDAFFE9A13C729672628C39D30ACD8B64E0289`이었다. |
| 반실사 계약의 구 규칙 검색: `정식 3앵커`, `최대 2장`, `01 + 03`, `02 + 03`, 구 02·03 파일명 | 통과. 일치 항목 없음. |
| 스타일 루트의 구 공통 마감 블록 검색 | 통과. `Apply the locked refined painterly digital illustration finish` 및 `Do not use watercolor washes` 일치 항목 없음. |
| 네 Markdown 파일의 코드 펜스 짝수 검사 | 통과. `MARKDOWN_CODE_FENCES: balanced`. |

## 자체 검토

- 붓터치 반실사 앵커는 얼굴·연령·전신 비례·마감으로 역할을 분리했고, 산출물 조합과 참조 한도 축소 판단을 중앙 문서에만 두었다.
- 제공 이미지 그림체 보존은 정식 앵커와 두 고정 영어 블록을 자동 삽입하지 않도록 명시했다.
- 정식 스타일 적용은 실제 픽셀 확인, 마감 앵커 유지, 그림체 블록 뒤의 공통 마감 블록 단일 삽입을 필수로 명시했다.
- 애니 페인팅은 기존 세 활성 파일과 얼굴·전신·마감 역할을 보존했으며, 이번 작업에서 4앵커 체계로 변환하지 않았다.
- 스타일 앵커의 인물·의상·팔레트·포즈·카메라·배경·장면 구성 복제를 금지했다.

## 우려 사항

- 보고서 작성 직전 보관본 SHA-256의 초기 콘솔 출력이 열 너비 때문에 잘려, 잘린 값을 수동 전사한 첫 재검증이 실패했다. 실제 파일을 다시 해시해 정확한 값으로 재검증했으며, 이동 직후의 원본 대비 해시 검증도 통과했다.
- Task 1 범위 밖의 모듈 루트·공통 승인·점검·내부 1~8번 기능 문서는 수정하지 않았다. 이후 Task에서 중앙 호출 규칙을 해당 문서들에 연결해야 전체 설계의 모듈 전파 기준을 충족한다.

## 수정 라운드 1

### 수정 내용

- 캐릭터 중심 세션 하이라이트의 반실사 조합 적용을 `붓터치 반실사를 명시적으로 선택한 경우`로 한정했다. 제공 이미지가 있고 명시적 정식 스타일 변환 요청이 없으면 `제공 이미지 그림체 보존`을 적용하며 정식 앵커 조합을 적용하지 않는다고 명시했다.
- 디자인 기준 이미지와 마감 앵커 뒤에 참조 한 자리만 남을 때의 3·4순위 예외를 명시했다. 명시 연령이 미성년·고령이거나 연령 오차가 핵심 위험이면 나이 표현 앵커를 우선하고 주 구도 앵커를 제외하며, 그 밖의 경우에는 주 구도 앵커를 우선한다.

### 검증 명령과 실제 결과

```powershell
$target = '1. 프롬프트\1. 이미지 생성 모듈\라이브러리\스타일\정식 스타일 호출 규칙.md'
rg -n -C 2 '붓터치 반실사를 명시적으로 선택한 경우|명시적 정식 스타일 변환 요청이 없는 세션 하이라이트|디자인 기준 이미지와 마감 앵커 뒤에 한 자리만 남고|3·4순위를 뒤집어 나이 표현 앵커를 우선|그 밖의 경우에는 주 구도 앵커를 우선' $target
$fenceCount = (Select-String -LiteralPath $target -Pattern '^```' -AllMatches).Count
if (($fenceCount % 2) -ne 0) { throw "코드 펜스 짝이 맞지 않습니다: $fenceCount" }
```

실제 결과:

- `rg -n -C 2`가 65행의 세션 하이라이트 조건과 75행의 한 자리 참조 예산 예외·기본 규칙을 모두 반환했다.
- 코드 펜스 검사 결과는 `CODE_FENCES: balanced (0)`이었다.

## 최종 종합 검토 수정 라운드 1

### 수정 내용

- `애니 페인팅 기본 조합`을 중앙 호출 규칙에 추가했다. 얼굴·흉상은 `01 + 03`, 반신·전신은 `02 + 03`, 비인물 풍경은 명시적 적용일 때 `03`만 사용한다.
- 애니 페인팅도 세 중앙 그림체 적용 모드를 따르며, 제공 이미지 보존·명시적 정식 스타일 변환·명시적 세션 하이라이트 선택·풍경 적용을 반실사와 같은 경계로 판정하도록 했다. 앵커 콘텐츠 복제 금지도 명시했다.
- 참조 예산의 나이 표현 앵커 우선 예외를 독립 나이 표현 앵커를 선언한 스타일에만 한정했다. 애니 페인팅은 `02. 중립 전신 앵커.png`에 연령별 체형 기준이 내장되어 있고 독립 나이 표현 앵커가 없으므로 이 예외를 적용하지 않는다.

### 검증 명령과 실제 결과

```powershell
$target = '1. 프롬프트\1. 이미지 생성 모듈\라이브러리\스타일\정식 스타일 호출 규칙.md'
rg -n -C 2 '## 붓터치 반실사 기본 조합|## 애니 페인팅 기본 조합|얼굴·흉상: 01 \+ 03|반신·전신: 02 \+ 03|비인물 풍경: 명시적 적용일 때 03만 사용|애니 페인팅도 그림체 적용 방식의 세 중앙 모드 원칙|독립 나이 표현 앵커를 선언한 스타일|애니 페인팅은 02 전신 앵커 안에 연령별 체형 기준이 내장' $target
$fenceCount = (Select-String -LiteralPath $target -Pattern '^```' -AllMatches).Count
if (($fenceCount % 2) -ne 0) { throw "코드 펜스 짝이 맞지 않습니다: $fenceCount" }
```

실제 결과:

- 조합 검색이 59행의 반실사 기본 조합, 67~73행의 애니 페인팅 기본 조합과 중앙 모드 조건, 80행의 독립 나이 표현 앵커 한정, 83행의 애니 페인팅 예외 제외를 모두 반환했다.
- 코드 펜스 검사 결과는 `CODE_FENCES: balanced (0)`이었다.
