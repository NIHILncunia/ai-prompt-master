# Task 3 검토 패키지

## 권위 자료

- 작업 브리프: `task-3-brief.md`
- 승인 설계: `../../../docs/superpowers/specs/2026-09-06-image-style-anchor-resolver-design.md`
- 중앙 호출 규칙: `../../../1. 프롬프트/1. 이미지 생성 모듈/라이브러리/스타일/정식 스타일 호출 규칙.md`
- 구현 보고서: `task-3-report.md`
- 수정 전 매니페스트: `task-3-before-manifest.json`
- 수정 전 스냅샷: `task-3-before/`

## 파일별 수정 차이

`task-3-diffs/`의 `01`부터 `13`까지 모든 `.diff` 파일이 권위 검토 범위다. 번호는 `task-3-before-manifest.json`의 파일 순서와 같다.

## 필수 보존 계약

- 포트레이트: `전신 9:16 → 전신 확정 → 흉상 9:16 또는 완성형 VTT 토큰 1:1`
- 완성형 VTT 토큰: 기준 이미지 그림체 보존, 캐릭터 스타일 앵커 재렌더링 금지
- V1: 기존 시트 출력 계약 유지
- 세션 하이라이트: 제공 이미지 기본 그림체 보존과 충돌 시 단일 기준 확인
- V2: `1916 × 821 px`, 기존 패널 배치·무기 규칙·전체 검은 데이터 바 유지

## 구현자 검증 요약

- 네 기능에서 중앙 호출 규칙과 그림체 보존 경로 검색 성공
- 금지된 구 조합·정식 3앵커·최대 2장 규칙 검색 결과 0건
- 대상 13개 파일 존재 및 Markdown 펜스 균형 통과

## 검토 요청

1. Task 3 브리프와 승인 설계를 기준으로 13개 diff와 현재 파일을 모두 검토한다.
2. 각 기능이 중앙 호출 규칙을 직접 읽고 정확한 구도 분류와 제공 이미지 보존 규칙을 갖는지 확인한다.
3. 위 필수 보존 계약이 약화되거나 변경되지 않았는지 확인한다.
4. 문제는 `Critical`, `Important`, `Minor`로 분류하고 파일·줄·근거·수정 방향을 제시한다.
5. 문제가 없으면 `Approved`라고 명시한다.
