# 이미지 생성 모듈 최종 전환 삭제 전 검증 보고서

## 작업 ID

`2026-08-08-image-generation-module`

## 목적

구 이미지 기능 폴더 8개를 제거하기 전에, 역사 기록과 보관을 제외한 현행 운영 문서가 구 경로를 참조하지 않고 신규 이미지 모듈만으로 연결되는지 검증한다.

## 검색 범위

- 포함: `1. 프롬프트` 아래의 활성 운영 Markdown 247개
- 제외: `보관/`, 구 이미지 기능 폴더 8개 자체
- 역사 Superpowers 설계·계획·감사·인계 문서는 프롬프트 루트 밖에 보존하며 검색 판정에서 제외했다.

## 초기 결과와 정리

초기 검사에서는 활성 문서 5개에서 구 이미지 폴더 참조 25건을 발견했다.

| 문서 | 초기 참조 수 | 처리 |
| --- | ---: | --- |
| `AGENTS.md` | 8 | 전환기 실제 폴더 매핑과 구 이미지 목록을 제거하고, 최종 활성 번호 체계만 남김 |
| `4. 로그 하이라이트 추출 프롬프트/0. 로그 하이라이트 추출 마스터 프롬프트.md` | 11 | 구 2·3·5번 이미지 호출을 이미지 모듈 내부 2·3·4번으로 교체 |
| `4. 로그 하이라이트 추출 프롬프트/1. 로그 하이라이트 추출 지침서.md` | 6 | 구 2·3·5번 이미지 호출을 이미지 모듈 내부 2·3·4번으로 교체 |
| `1. 이미지 생성 모듈/1. 반실사 포트레이트/라이브러리/반실사 캐릭터 프리셋 라이브러리.md` | 1 | 구 캐릭터 레퍼런스 경로를 내부 3번 경로로 교체 |
| `공통/종족 데이터/3. 종족 데이터 점검 및 충돌 처리 수칙.md` | 1 | 존재하지 않게 될 구 포트레이트 종족 데이터 경로를 하위 기능 폴더 중복 금지 규칙으로 교체 |

## 재검사 결과

동일 범위에서 구 이미지 기능 폴더명 8종을 다시 검색했다.

```text
OPERATING_SCAN_FILES=247
ACTIVE_LEGACY_REFERENCE_HITS=0
```

구 이미지 폴더 제거 뒤에도 현재 운영 라우팅이 해당 폴더를 호출하지 않는 상태다.

## 신규 이미지 모듈 이관 확인

신규 이미지 모듈 내부 1~8번을 다시 확인했다.

```text
NEW_IMAGE_FEATURE_COUNT=8
INCOMPLETE_NEW_IMAGE_FEATURES=0
```

각 내부 기능은 AGENTS, README, 0번 마스터를 모두 갖춘다.

## 별도 발견 사항

다음 문서에는 신규 붓터치 반실사 계약과 충돌할 수 있는 과거 3D 렌더링 표현 두 건이 남아 있다.

- `1. 이미지 생성 모듈/1. 반실사 포트레이트/라이브러리/반실사 캐릭터 프리셋 라이브러리.md:286`
- `1. 이미지 생성 모듈/1. 반실사 포트레이트/라이브러리/반실사 캐릭터 프리셋 라이브러리.md:312`

이 표현은 구 이미지 폴더 경로 의존성이 아니므로 삭제 전 게이트를 막지는 않는다. 다만 신규 포트레이트 품질·금지 규칙이 3D CGI/AAA 강제 스타일을 폐기한다고 명시하므로, 별도 문체·프롬프트 정합성 보정 작업으로 처리해야 한다.

## 판정

Task 3의 삭제 전 운영 참조 0건 게이트를 통과했다. 기준선 백업, 신규 모듈 정본 완결, 활성 운영 참조 0건이 모두 확보됐으므로, 다음 Task 4에서 구 이미지 기능 폴더 8개만 제거할 수 있다.
