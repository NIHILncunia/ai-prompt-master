# PNG-WebP 이미지 포맷 정규화 검증 보고서

## 검증 범위

- 실제 작업 루트: `C:\Users\nihil\coding\ai\ai-data-master\TRPG 데이터`
- 기준 브랜치: `master`
- 최초 실제 PNG: 334개
- 최초 실제 WebP: 137개
- 최초 실제 동일 stem PNG-WebP 쌍: 0개
- 선행 미커밋 변환: 추적 PNG 삭제 27개와 같은 stem WebP 27개

## 변환 결과

| 항목 | 결과 |
| --- | ---: |
| 이번 실행에서 변환·검증한 PNG | 334개 |
| 기존 동일 stem WebP와 동일하여 PNG만 제거 | 0개 |
| 선행 미커밋 WebP의 Git 원본 대비 검증 | 27개 통과 |
| APNG | 0개 |
| 충돌 | 0개 |
| 변환 실패 | 0개 |
| 검증 뒤 제거한 PNG | 334개 |
| 최종 실제 PNG | 0개 |
| 최종 실제 WebP | 471개 |
| WebP 읽기 실패 | 0개 |
| WebP 0 byte | 0개 |
| 해상도 불일치 | 0개 |

내부 인코딩 처리량 조정 과정에서 먼저 생성한 10개는 같은 실행 매니페스트에서 다시 원본과 비교해 재사용했다. 이는 작업 시작 시 존재하던 동일 stem 충돌에 포함하지 않는다.

## 픽셀과 alpha 검증

- PNG 334개를 RGBA로 디코딩해 WebP와 width·height, alpha 전체, alpha가 0보다 큰 가시 픽셀의 RGB를 비교했다.
- 334개 모두 일치했고 실패는 0개다.
- 원본 모드는 RGB 313개, RGBA 21개다.
- alpha 채널 보유 원본은 21개이며 실제 투명 픽셀 보유 원본은 2개다.
- 선행 미커밋 27개도 Git 원본 PNG와 같은 기준으로 비교했으며 alpha 채널 보유 6개, 실제 투명 픽셀 보유 3개를 포함해 모두 통과했다.
- 토큰 WebP 17개는 모두 1254 × 1254 정사각형 RGBA다. 16개는 네 모서리 alpha가 0이다.
- `6. NPC 이미지/중급 물의 정령 오르마 토큰.webp`는 원본부터 두 모서리 alpha가 1이고 나머지 두 모서리가 0인 상태이며, 변환본에 같은 값이 정확히 보존됐다. 재인코딩 외 이미지 편집 금지 조건에 따라 원본 alpha를 임의 보정하지 않았다.

## 메타데이터 검증

- 메타데이터 보유 원본은 8개다.
- XMP 보유 4개는 byte 단위로 일치한다.
- EXIF 보유 4개는 WebP 컨테이너 직렬화에서 헤더 길이가 달라졌으나, Git 원본과 대상 WebP의 EXIF 태그 3개씩을 비교한 결과 키와 값이 모두 일치한다.
- ICC profile 보유 원본은 없었다.

## 참조와 활성 규칙 검증

- 실제 현재 자산 참조 6건을 3개 파일에서 WebP 경로로 수정했다.
- 현재 로컬 자산을 가리키는 삭제된 PNG 참조는 0건이다.
- 잔존 dot-png 문자열은 181줄이며 외부 URL 0줄, 역사 기록 176줄, 이번 마이그레이션 기술 설명 5줄, 수정 누락 0줄로 분류했다.
- 신규 WebP 출력 규칙을 추가 보정한 활성 지침은 다음 4개다.
  1. `1. 프롬프트/1. 이미지 생성 모듈/README.md`
  2. `1. 프롬프트/1. 이미지 생성 모듈/공통/1. 이미지 생성 공통 승인 절차.md`
  3. `1. 프롬프트/1. 이미지 생성 모듈/공통/2. 요구사항 확인서 작성 규칙.md`
  4. `1. 프롬프트/1. 이미지 생성 모듈/1. 반실사 포트레이트/라이브러리/토큰 계열 프로필 운영 지침.md`
- 전신·초상화·토큰 저장 파일명은 각각 `<액터>.webp`, `<액터> 초상화.webp`, `<액터> 토큰.webp`로 확인했다.
- 토큰 계약은 1:1, 원형 프레임, 프레임 바깥 투명, WebP alpha로 확인했다.
- 스타일 앵커의 활성 참조는 실제 WebP 파일명과 일치한다.

## 로컬 경로 명칭 전환

- 활성 지침 3개 파일의 이전 저장소명 참조 5건을 `ai-data-master`로 전환했다.
- 이전 저장소명 잔존 문자열은 0건이다.
- 대상 파일의 `ai-data-master` 경로 5건을 재검색해 확인했다.

## 대표 샘플 육안 검증

다음 8개를 직접 열어 크롭·색상·형태 손상과 투명 배경을 확인했다.

1. NPC 전신: `6. NPC 이미지/로지니아 마스테.webp`
2. NPC 초상화: `6. NPC 이미지/로지니아 마스테 초상화.webp`
3. NPC 토큰: `6. NPC 이미지/로지니아 마스테 토큰.webp`
4. 몬스터: `10. 몬스터 이미지/04. 구조체/엘-드로이드/칼-드라크/강탈자 칼-드라크 초상화.webp`
5. 배틀맵: `11. 맵 이미지/ChatGPT Image 2026년 6월 26일 오전 11_05_53.webp`
6. 스타일 앵커: `1. 프롬프트/1. 이미지 생성 모듈/라이브러리/견본/붓터치 반실사/01. 핵심 얼굴 앵커.webp`
7. 아이콘: `15. 이미지 생성 결과/아이콘/흑룡의 비늘 아이콘.webp`
8. 투명 배경: `15. 이미지 생성 결과/캐릭터/강탈자 칼-드라크 포트레이트 토큰.webp`

모든 샘플이 정상 표시됐으며 눈에 띄는 재인코딩 손상은 확인되지 않았다. 투명 배경 샘플의 프레임 바깥 영역도 투명하게 표시됐다.

## Git 검증

- `git diff --check -- .`: 통과
- 기존 미커밋 변경은 stash, reset, checkout 또는 복원하지 않았다.
- `TRPG 데이터` 밖의 형제 폴더는 이번 작업에서 수정하지 않았다.
