# 엔터티 자산 폴더 전환 검증 보고서

## 판정

- 판정: 통과
- 검증일: 2026-09-09
- 범위: 기존 이미지 전용 루트 13개, 대상별 자산 폴더, 활성 프롬프트·세션의 자산 경로

## 자산 보존

| 항목 | 기준선 | 검증 결과 |
| --- | ---: | ---: |
| 이미지 파일 | 536개 | 536개 |
| SHA-256 다중집합 | `9596AEEF70F76C7602E47C82B1FD6629DFB4416D6B389F7218E95CAE6B4A6868` | 일치 |
| 자산 인덱스 | - | 283개 |
| 유효 UUID v4 | - | 283개 |
| UUID 중복 | - | 0개 |
| 기존 이미지 전용 루트 잔존 | 0개가 목표 | 0개 |

기준선은 `2026-09-09-entity-asset-folder-migration-baseline.md`에 보관한다. 이미지 픽셀은 재생성·변환·덮어쓰기 없이 이동만 수행했다.

## 분류 결과

- 대상 폴더에 자산 인덱스가 있는 이미지: 493개
- `16. 미분류`에 보류한 이미지: 43개
- 미분류 보류 사유: 정체 미확정 PC 후보, 신격 파일명 미확정, 전도용 축약 대륙, 스타일 앵커·마감 비교 시험 결과 등으로 대상 연결을 확정할 근거가 없다.
- `16. 미분류/15. 이미지 생성 결과/`는 이전 루트가 아니라 현재 미분류 자산의 출처 보존 하위 폴더다. 이 경로의 이름 때문에 단순 문자열 검사에 3건이 잡히지만, 이전 최상위 `15. 이미지 생성 결과` 루트는 존재하지 않는다.

## 운영 문서와 세션 참조

- 전역·프롬프트·이미지 생성·하이라이트·종족 시각 자료 지침을 대상 자산 폴더 기준으로 전환했다.
- 레퍼런스 시트 저장명은 `<대상> 레퍼런스 시트 V1.webp`와 후속 V2 이상으로 통일했다.
- 활성 세션 `섬에서 전해진 바람 상편 2`의 수상한 마법사와 인형사의 실타래 자산 경로를 실제 새 위치로 갱신했다.
- `엘 드로이드 가이드.md`를 `12. 종족/엘-드로이드/`로 이동해 종족 자산과 동거시켰다.

## 정리 상태

- 과거 이미지 전용 루트 13개는 모두 존재하지 않는다.
- 이관 중 생성된 빈 중첩 폴더 5개는 실제 파일이 없음을 확인했다. 실행 환경의 삭제 정책으로 제거 명령이 차단되어 남아 있을 수 있으나, 자산·문서 참조·운영에는 영향이 없다.

## 형식 검사

- `git diff --check`를 실행했다.
- 현재 작업 트리에 있던 다른 작업의 trailing whitespace 4건과 EOF 4건이 출력되었다. 이관 작업에서 직접 수정하지 않은 기존 더티 항목이므로 변경하지 않았다.
- Git의 LF/CRLF 경고는 줄 끝 문자 변환 예고이며 내용 검증 실패가 아니다.
