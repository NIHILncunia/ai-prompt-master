# 이미지 생성 모듈 Phase F 구 경로·구 번호 참조 감사

## 작업 ID
`2026-08-08-image-generation-module`

## 목적
기존 상위 이미지 기능 1·2·3·5·10·12·13·14번을 제거하기 전에 현재 운영 문서에서 해당 경로와 번호를 신규 `1. 이미지 생성 모듈` 내부 1~8번으로 전환한다.

## 전역 검색 결과
현재 운영에 직접 영향을 주는 핵심 수정 대상은 다음 5개 문서로 판정했다.

- `TRPG 데이터/AGENTS.md`
- `TRPG 데이터/1. 프롬프트/AGENTS.md`
- `TRPG 데이터/1. 프롬프트/00. TRPG 세션 매니저 프로젝트 기본 지침.md`
- `TRPG 데이터/1. 프롬프트/0. TRPG 데이터 프롬프트 통합 마스터 프롬프트.md`
- `TRPG 데이터/1. 프롬프트/1. 이미지 생성 모듈/공통/1. 이미지 생성 공통 승인 절차.md`

과거 Superpowers 보고서, 과거 마이그레이션 설계, 표준화 기준 문서에 남은 구 번호는 역사 기록이므로 수정하지 않는다.

## 신규 라우팅
- 구 1 → `1. 이미지 생성 모듈/1. 반실사 포트레이트`
- 구 2 → `1. 이미지 생성 모듈/2. 배틀맵 생성`
- 구 3 → `1. 이미지 생성 모듈/3. 캐릭터 레퍼런스 생성`
- 구 5 → `1. 이미지 생성 모듈/4. 세션 하이라이트 이미지 생성`
- 구 10 → `1. 이미지 생성 모듈/5. 토큰 프레임 생성`
- 구 12 → `1. 이미지 생성 모듈/6. 풍경 생성`
- 구 13 → `1. 이미지 생성 모듈/7. 게임 아이콘 생성`
- 구 14 → `1. 이미지 생성 모듈/8. 월드 지도 생성`

## 포트레이트 계약 정리
상위 문서에 남아 있던 전신/초상화 완전 독립 계약을 제거하고 신규 정본 계약으로 통일했다.

`전신 9:16 → 전신 확정 → 확정 전신 기반 동일 개체 흉상 9:16`

단, 전신 생성 호출과 흉상 생성 호출은 각각 공통 승인 하드 게이트를 개별 적용한다.

## 삭제 후보
Phase F 수정 ZIP 반영 후 전역 검색에서 현재 운영 문서의 구 경로 참조가 제거되면 다음 상위 폴더를 삭제 후보로 확정할 수 있다.

- `1. 포트레이트 생성 프롬프트`
- `2. 배틀맵 생성 프롬프트`
- `3. 캐릭터 레퍼런스 시트 생성 프롬프트`
- `5. 세션 하이라이트 이미지 생성 프롬프트`
- `10. 토큰 프레임 생성 프롬프트`
- `12. 풍경 일러스트 생성 프롬프트`
- `13. 게임 아이콘 생성 프롬프트`
- `14. 월드 지도 생성 프롬프트`

## 다음
1. 수정 ZIP을 사용자 반영
2. 신규 운영 문서 존재 및 내용 확인
3. 구 경로를 다시 전역 검색
4. 역사 문서를 제외한 현재 운영 참조가 0인지 확인
5. 구 이미지 기능 폴더 삭제 목록 확정
6. Phase G 비이미지 기능 전체 재번호 진입
