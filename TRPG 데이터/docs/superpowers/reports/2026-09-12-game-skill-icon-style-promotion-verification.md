# 게임 스킬 아이콘 정규 스타일 이식 검증 보고서

- 검증일: 2026-09-12
- 범위: `게임 스킬 아이콘` 개발 패키지의 정식 스타일 승격, 정식 앵커, 7번 게임 아이콘 런타임 문서, 중앙 스타일 호출 규칙, 3×3 패키징 계약
- 판정: 통과

## 1. 정식 앵커 검증

정식 경로:
`1. 프롬프트/1. 이미지 생성 모듈/라이브러리/견본/게임 스킬 아이콘/`

검증한 5개 앵커:
1. `01. 동적 원소 공격 앵커.png`
2. `02. 장축 투사체 앵커.png`
3. `03. 고에너지 발광 앵커.png`
4. `04. 치유·지원 앵커.png`
5. `05. 방어·재질 앵커.png`

검증 결과:
- 정식 앵커 수: 5
- 개발 앵커 수: 5
- 해상도: 전부 정확히 512×512
- 모드: 전부 RGB
- 투명 픽셀: 없음
- 개발본 ↔ 정식본 SHA-256: 5/5 일치

## 2. 런타임 의존성 검증

활성 게임 아이콘 기능 문서와 중앙 스타일 문서에서 다음 개발 작업 경로의 직접 런타임 의존을 검색했다.

`20. 그림 스타일 분석/게임 스킬 아이콘 스타일`

결과: 0건.

따라서 개발 패키지는 승격 근거로만 보존되고 런타임은 정식 스타일 계약과 정식 앵커를 사용한다.

## 3. 구형 배경·패키징 규칙 제거 검증

다음 구형 표현을 활성 런타임 문서에서 검색했다.

- `아이템이 아닌 아이콘은 회색 또는 검은색`
- `아이템이 아니면 회색 또는 검은색`
- `그 외 유형은 회색 또는 검은색`
- `isolated on a plain matte neutral dark background`
- `사용자가 시트를 요청하지 않았다면 그리드나 합성 시트가 아니다`

결과: 전부 0건.

현재 활성 계약은 실제 아이콘 슬롯 전체를 피사체·속성과 조화되는 유색 비투명 추상 배경으로 채우며, 순수 검정은 비어 있는 3×3 슬롯에만 사용한다.

## 4. 필수 런타임 파일 존재 검증

다음 파일을 확인했다.

- `라이브러리/스타일/게임 스킬 아이콘.md`
- `라이브러리/스타일/정식 스타일 호출 규칙.md`
- `7. 게임 아이콘 생성/AGENTS.md`
- `7. 게임 아이콘 생성/0. 게임 아이콘 마스터 프롬프트.md`
- `7. 게임 아이콘 생성/1. 게임 아이콘 공통 지침.md`
- `7. 게임 아이콘 생성/2. 아이콘 스타일 규칙.md`
- `7. 게임 아이콘 생성/3. 출력 구성 규칙.md`
- `7. 게임 아이콘 생성/4. 용도별 아이콘 분기.md`
- `7. 게임 아이콘 생성/5. 게임 아이콘 실패 방지 체크리스트.md`
- `7. 게임 아이콘 생성/라이브러리/아이콘 유형 및 시각 언어 라이브러리.md`

결과: 전부 존재.

## 5. 대표 패키징 케이스 재검증

현행 계약:
- 슬롯 512×512
- 시트 1536×1536
- 3×3
- 행 우선 배치
- 셀 사이·외곽 0px
- 실제 아이콘이 없는 슬롯만 순수 검정

대표 케이스:
- 1개: 기본 낱개 512×512. 시트가 필요하면 1개 실제 슬롯 + 8개 빈 슬롯.
- 5개: 1시트, 5개 실제 슬롯 + 4개 빈 슬롯.
- 9개: 1시트, 9개 실제 슬롯 + 빈 슬롯 0개.
- 11개: 2시트, 첫 시트 9개 + 둘째 시트 2개 실제 슬롯·7개 빈 슬롯.

판정: 모두 현행 계약과 일치.

## 6. Markdown 구조 검증

정식 런타임 문서와 게임 스킬 아이콘 개발 패키지의 Markdown 코드 펜스를 정적 검사했다.

- 검사 파일: 30개
- 펜스 불균형: 0건

## 7. 최종 판정

게임 스킬 아이콘 스타일은 개발 패키지에서 정식 런타임 구조로 이식되었고, 정식 앵커 5장과 기능 전용 스타일 계약, 중앙 호출 규칙, 게임 아이콘 QA·패키징·배경 규칙이 서로 일치한다.

스킬 아이콘 정규 이식 단계의 검증 결과는 `통과`다.

후속 작업은 별도 설계 게이트를 거치는 `무기 아이콘 스타일` 개발이며, 무기 스타일 안정화 뒤 `장비 아이콘 스타일`로 확장한다.
