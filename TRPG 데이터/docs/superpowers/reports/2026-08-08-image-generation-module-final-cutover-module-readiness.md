# 이미지 생성 모듈 최종 전환 정본 완결 검증 보고서

## 작업 ID

`2026-08-08-image-generation-module`

## 목적

구 이미지 폴더 삭제와 비이미지 기능 재번호 전에, 신규 `1. 이미지 생성 모듈`이 상위 문서와 내부 하위 기능에서 단독 정본으로 작동할 수 있는지 보정하고 검증한다.

## 원인 판정

이미지 모듈 신규 구조 작성과 상위 운영 문서 갱신이 같은 전환 배치로 끝나지 않아, 일부 상위 문단에는 새 경로·고정 계약이 들어갔지만 다른 문단에는 이전 경로·단일 실행 계약이 남아 있었다. 내부 5~8번 AGENTS도 폴더만 생성된 뒤 얇은 라우터로 완결되지 않은 상태였다.

## 적용 내용

### 1. 공통 승인 경로 통일

다음 두 상위 문서에서 중첩된 잘못된 경로 6건을 실제 경로로 교체했다.

```text
1. 이미지 생성 모듈/1. 이미지 생성 모듈/공통/1. 이미지 생성 공통 승인 절차.md
→ 1. 이미지 생성 모듈/공통/1. 이미지 생성 공통 승인 절차.md
```

- `00. TRPG 세션 매니저 프로젝트 기본 지침.md`
- `0. TRPG 데이터 프롬프트 통합 마스터 프롬프트.md`

### 2. 포트레이트 계약 통일

두 상위 문서에 남은 “전신 또는 초상화 1장만” 규칙을 다음 정본 계약으로 통일했다.

```text
전신 9:16 1장 → 전신 확정 → 동일 개체 흉상 9:16 1장
```

전신과 흉상은 각각 요구사항 확인서와 명시적 승인을 거치는 별도 이미지 생성 호출로 명시했다.

### 3. Codex 로컬 접근 규칙 통일

`TRPG 데이터/AGENTS.md`의 Drive 접근 시작 문구 강제를 제거하고, 동기화된 로컬 Markdown을 직접 읽으며 해당 문구와 Drive 커넥터를 사용하지 않는 규칙으로 통일했다.

### 4. 내부 5~8번 AGENTS 완결

다음 네 AGENTS에 역할, 읽기 순서, 공통 승인 절차, 기능 경계 또는 핵심 계약을 추가했다.

- 내부 5번 토큰 프레임 생성
- 내부 6번 풍경 생성
- 내부 7번 게임 아이콘 생성
- 내부 8번 월드 지도 생성

## 검증 결과

| 검증 항목 | 결과 |
| --- | ---: |
| 중첩 공통 승인 경로 | 0건 |
| 이전 단일 포트레이트 실행 규칙 | 0건 |
| 최상위 Drive 시작 문구 강제 | 0건 |
| 읽기 순서·마스터·공통 승인·계약이 모두 없는 내부 AGENTS | 0개 |
| 이번에 AGENTS가 가리킨 명시 대상 파일 | 모두 존재 |

## 판정

Task 2의 신규 이미지 모듈 정본 완결 게이트를 통과했다. 구 이미지 폴더는 아직 모두 존재하며, 삭제와 재번호는 이 단계에서 수행하지 않았다.
