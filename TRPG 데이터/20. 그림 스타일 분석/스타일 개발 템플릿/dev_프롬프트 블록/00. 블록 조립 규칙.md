# 00. 블록 조립 규칙

- 모든 블록은 독립적으로 테스트 가능해야 한다.
- 역할이 겹치는 문장은 가능한 한 한 블록에만 둔다.
- 테스트 프롬프트는 `01 기본 스타일`을 필수로 하고 필요한 블록만 추가한다.
- 최종 정식 스타일 계약은 검증된 블록을 하나의 고정 영어 블록 또는 명확한 고정 블록 세트로 조립한다.
- 블록 파일은 개발용이며 정식 런타임에서 직접 호출하지 않는다.
