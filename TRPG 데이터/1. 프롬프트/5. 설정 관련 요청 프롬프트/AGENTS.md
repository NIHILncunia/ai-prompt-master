# 5번 설정 관련 요청 기능 지침

## 폴더 정체성

- 현재 폴더: `TRPG 데이터/1. 프롬프트/5. 설정 관련 요청 프롬프트`
- 폴더 ID: `1YFLnQS2k9hcjeEHrDEpSVFQ4dk_Qhj4C`
- 상위 프롬프트 루트 ID: `1PDa8TO0BdKrPaNWTfImJTcYoYY3gpj8N`
- 상위 TRPG 데이터 루트 ID: `15dR_0fhsY5RwmsJkr18FERKLMvqjTJaH`
- 기능 진입점: `0. 설정 관련 요청 마스터 프롬프트.md`

## 역할

프로젝트 위그드라실의 설정 조회·작성·점검·윤문·정합성·템플릿 적용을 라우팅한다.

## 필수 읽기 순서

1. 상위 `../AGENTS.md`
2. 상위 `../00. TRPG 세션 매니저 프로젝트 기본 지침.md`
3. 상위 `../0. TRPG 데이터 프롬프트 통합 마스터 프롬프트.md`
4. `0. 설정 관련 요청 마스터 프롬프트.md`
5. `1. 설정 관련 요청 지침서.md`
6. `2. 설정 문서 점검 및 밀도 분석 수칙.md`
7. `3. 설정 문서 템플릿 구조 점검 수칙.md`
8. 대륙 정본 또는 대륙 내부 요소의 위치·범위·인접·소속 권역·연결 관계를 다루면 `4. 대륙 공간 배치 기준 운용 수칙.md`

요청과 무관한 예시·라이브러리·데이터 폴더는 무조건 전부 읽지 않는다. 마스터가 지정하거나 현재 작업에 필요한 자료만 추가로 확인한다.

설정 정본 루트 ID: `1Wjo6z5YfZDTjzfwNNVhR4ubZ2CVhH6oj` · 룩스테라: `16BFlKv3Y-5z1Wm7NVm_PZBITFOd275nx` · 엘드로스: `1N7fXCdeYAAnyqhgN0GGAU5-_QboPNv5P` · 위그드라실 공용: `1yEup6FPaB4GidbdSPZGYn1KfEnmIrT--`

## 설정 문서 분류 경로

- 룩스테라·엘드로스의 일반 설정 문서는 `설정/<type>/문서.md`에 둔다.
- 룩스테라 `region: 청연` 문서는 `설정/청연/<type>/문서.md`에 둔다.
- 문서명으로 찾을 때에는 대상 시리즈 `설정` 폴더를 재귀 검색하고, `type` 또는 청연 지역값이 확인되면 해당 경로를 우선한다.

## 기능 경계와 연동

- 설정 정본 진입점은 프로젝트 위그드라실 루트다.
- 종족 데이터 구조는 `공통/종족 데이터/`, 특성·능력·클래스 설계는 각각 7번 창작 지원의 특성·능력·클래스 제작기와 경계를 유지한다.
- 문체·밀도 규칙은 설정 루트 문서 작성 및 수정 지침과, 템플릿 규칙은 설정 루트 템플릿 구조 점검 수칙과, 대륙 공간 배치 규칙은 설정 루트 대륙 공간 배치 기준 작성 지침과 동기화한다.

## 동기화 원칙

- 이 `AGENTS.md`와 `0. 설정 관련 요청 마스터 프롬프트.md`는 함께 기능 진입점으로 취급한다.
- 폴더 ID·경로·진입점·읽기 순서·기능 경계·외부 데이터 원천·기본 산출물·파일 정책이 바뀌면 두 문서를 함께 점검한다.
- 세부 실행 절차와 품질 규칙은 마스터와 하위 지침에 둔다. 상위 전역 규칙이나 다른 기능이 소유한 정본을 이 폴더에 복제하지 않는다.
- 전역 라우팅이나 파일 정책이 바뀌면 TRPG 데이터 루트·프롬프트 루트 `AGENTS.md`, 프로젝트 기본 지침, 통합 마스터를 함께 갱신한다.
- 새 하위 문서가 진입점·필수 로드 파일·정본 데이터 원천이 되면 이 문서와 마스터의 파일 목록 및 읽기 순서를 함께 수정한다.
- 대륙 공간 배치 운용 절차를 바꾸면 `4. 대륙 공간 배치 기준 운용 수칙.md`, 설정 루트 `대륙 공간 배치 기준 작성 지침.md`, 관련 시리즈 `AGENTS.md`와 전용 템플릿을 함께 점검한다.

## 파일 작업 원칙

- Codex는 동기화된 로컬 Markdown을 직접 읽으며 Google Drive 커넥터를 호출하지 않는다.
- 수정본은 실제 적용 경로와 원래 파일명을 유지한 로컬 파일 또는 ZIP으로 제공한다.
- 기존 문서를 교체할 때에는 ZIP 내부 경로에 따라 덮어쓰고, 삭제가 필요한 파일은 별도 목록으로 명시한다.
