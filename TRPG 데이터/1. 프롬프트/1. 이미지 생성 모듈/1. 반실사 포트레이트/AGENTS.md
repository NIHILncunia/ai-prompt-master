# 1. 반실사 포트레이트 AGENTS

## 역할

이 폴더는 이미지 생성 모듈 내부 1번 기능인 `반실사 포트레이트`를 담당한다. 전신·흉상·완성형 VTT 토큰을 생성하며, 완성형 토큰의 두 입력 경로를 소유한다.

## 직접 준비 호출

- `포트레이트 준비`, `포트레이트 준비.`는 이 기능의 처리 프로세스 안내 요청이다. 별도 스타일 지정이 없으면 기존 기본 스타일을 사용한다.
- `애니 페인팅 포트 준비`, `애니 페인팅 포트 준비.`, `애니 페인팅 포트레이트 준비`, `애니 페인팅 포트레이트 준비.`는 이 기능의 처리 프로세스 안내 요청이며 선택 스타일을 `애니 페인팅`으로 고정한다. `애니 페인팅`은 정식 스타일명이다. 과거 명칭 `세미리얼 애니 페인팅`은 레거시 별칭으로만 받아들인다.
- 준비 호출이 아니더라도 포트레이트 요청에서 `애니 페인팅`, `애니 페인팅으로 전환`, `애니 페인팅 스타일`, `세미리얼 애니 페인팅`을 명시하면 같은 스타일을 선택한다.
- 직접 준비 호출에서는 요구사항 확인서 작성, 프롬프트 공개, 생성·수정·외부 실행을 시작하지 않는다.

## 읽기 순서

1. 상위 `../AGENTS.md`
2. 상위 `../0. 이미지 생성 모듈 마스터 프롬프트.md`
3. `0. 반실사 포트레이트 마스터 프롬프트.md`
4. `../공통/1. 이미지 생성 공통 승인 절차.md`
5. `1. 반실사 포트레이트 공통 지침.md`
6. `2. 전신 생성 지침.md`
7. `3. 흉상 생성 지침.md`
8. `4. 완성형 토큰 생성 지침.md`
9. 필요 시 `라이브러리/토큰 계열 프로필 운영 지침.md`
10. `../라이브러리/스타일/정식 스타일 호출 규칙.md`에서 그림체 적용 방식, 산출물 구도 분류와 실제 앵커 조합을 판정한다.
11. 선택 스타일 계약. `애니 페인팅`이면 `../라이브러리/스타일/애니 페인팅.md`를 읽고, 별도 스타일이 없으면 `../라이브러리/스타일/붓터치 반실사.md`를 읽는다.
12. 중앙 호출 규칙이 반환한 정식 앵커 이미지를 실제 픽셀로 확인한다.
13. `../라이브러리/스타일/공통 붓터치 마감 규칙.md`의 고정 영어 마감 블록을 읽는다.
14. 필요 시 대상 라이브러리
15. 요구사항 확인서와 영어 프롬프트 초안 작성 후 `../공통/6. 이미지 생성 프롬프트 점검 및 교정 지침.md`

## 핵심 계약

- `애니 페인팅`과 `붓터치 반실사` 생성은 정식 시각 앵커의 실제 이미지 픽셀 확인이 완료되어야 준비 완료로 본다. 파일명·메타데이터 확인만으로 대체하지 않는다.
- 요구사항 확인서와 최종 영어 프롬프트 검토에는 선택 스타일과 실제로 사용한 앵커의 전체 파일명을 기록한다.
- 최종 영어 프롬프트에는 선택 스타일의 고정 영어 그림체 블록과 공통 고정 영어 마감 블록을 문장 변경 없이 넣는다.
- 스타일 앵커의 인물·의상·팔레트·포즈·카메라·배경·구성은 참조하지 않는다.
- 신규 얼굴·흉상은 `붓터치 반실사` 선택 시 `01 + 02 + 04`, 신규 반신·전신은 `01 + 02 + 03 + 04`를 기본 호출한다. 실제 조합과 참조 예산 축소는 `../라이브러리/스타일/정식 스타일 호출 규칙.md`가 결정한다.
- 요구사항 확인서와 최종 영어 프롬프트는 공통 프롬프트 점검의 P01~P10을 모두 통과한 뒤 공개한다.

- 인물·NPC·PC·몬스터 작업의 기본 산출물은 전신 9:16, 초상화 9:16, 완성형 토큰 1:1의 3종 세트다. 전신을 먼저 확정하고 초상화와 토큰을 각각 독립 승인으로 순차 생성한다. 마스터가 일부만 요청하거나 제외한 경우에만 범위를 줄인다.
- 전신 9:16을 생성·확정한 뒤 동일 개체의 흉상 또는 완성형 토큰을 후속 산출물로 선택할 수 있으며, 기본 세트 작업에서는 둘 다 완료한다.
- 사용자가 이미지를 업로드하면 전신 생성을 선행하지 않고 해당 이미지를 기준으로 완성형 토큰을 직접 생성할 수 있다.
- 완성형 토큰은 기준 이미지의 캐릭터 본체를 그대로 사용하며, 원형 배치·미세 보정·자동 프레임·투명 처리만 적용한다.
- 완성형 토큰은 기준 이미지의 기존 그림체를 보존하며 캐릭터 스타일 앵커로 다시 그리지 않는다.
- 완성형 토큰은 캐릭터가 들어간 최종 토큰이며, 빈 프레임 또는 별도 프레임 WebP가 아니다.
- 토큰 계열은 대상 유형과 설정을 근거로 자동 선택하며, 등급은 일반·엘리트·보스로 처리한다. 등급이 없으면 일반 토큰이다.
- 각 이미지 생성은 공통 승인 절차를 개별 적용하고 마스터의 명시적 승인을 받은 뒤에만 실행한다.
- 업로드·저장 파일명은 `<액터>.webp`, `<액터> 초상화.webp`, `<액터> 토큰.webp`로 구분한다. 접미가 없는 `<액터>.webp`는 해당 액터의 전신 확정본을 의미하며 `전신` 접미사는 붙이지 않는다.
- **인물·NPC·PC·몬스터 이미지 작업의 기본 종료 지점은 생성이 아니라 Google Drive 저장이다.** 마스터가 저장 제외나 로컬 산출물만을 명시하지 않는 한, 생성·수정 → 검수/확정 → 최초 업로드 또는 교체 업로드까지 이어서 수행해야 완료로 본다.
- 인물 계열의 표준 이미지 세트는 `<액터>.webp` 전신, `<액터> 초상화.webp`, `<액터> 토큰.webp`의 **3종 세트**다. 마스터가 특정 산출물만 요청하거나 일부를 제외한 경우에만 범위를 축소한다. 각 이미지 생성 호출의 승인 상태는 기존 공통 승인 절차대로 서로 독립적으로 사용한다.
- 저장 분류는 설정상 역할을 우선한다. NPC는 `TRPG 데이터/6. NPC/<대상>/`, PC는 `TRPG 데이터/7. PC/<대상>/`, 몬스터는 `TRPG 데이터/10. 몬스터/<유형>/<대상>/`에 저장한다. 용종은 `TRPG 데이터/14. 용종/<유형>/<대상>/`에 두되 몬스터 자산 규칙을 그대로 따른다.
- 신규 정본이 없으면 최초 업로드한다. 같은 액터의 기존 정본이 있으면 **기존 Drive 파일 ID를 유지한 교체 업로드**를 기본으로 한다. 이미지 작업 안에서 마스터가 결과를 `픽스`, `확정`, `사용`, 다음 파생 산출물 진행 등으로 채택한 경우 저장·교체를 위해 별도의 `업로드하자` 지시를 다시 요구하지 않는다.
- NPC/PC/몬스터 분류가 자료만으로 판정되지 않을 때에만 저장 위치를 확인하며, 외형만으로 임의 분류하지 않는다.
