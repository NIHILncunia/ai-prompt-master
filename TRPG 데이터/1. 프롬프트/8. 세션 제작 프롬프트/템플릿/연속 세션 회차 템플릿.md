# 연속 세션 회차 패키지 템플릿

`단일 세션 템플릿.md`의 전체 구조를 사용한다.

시나리오 파일에는 다음 항목을 추가한다.

```markdown
## 이전 내용과 현재 상황
## 이번 회차에서 변화할 요소
## 아크 개요에 미치는 영향
```

`02. 장면 목록`에는 필요한 경우 `이전 회차 회수` 열을 추가한다. 맵 열은 단일 세션과 동일하게 유지한다.

각 장면 파일에는 필요한 경우 `이전 결과의 반영`을 구체적으로 기록한다.

`03. 등장인물 및 몬스터`와 `NPC 및 몬스터/`에서는 이전 관계와 기존 이미지·토큰·스탯블록 재사용 가능성을 먼저 확인한다.

`NPC 및 몬스터/[이름].md`의 `마스터용 묘사`도 현재 회차의 실제 등장 상태와 일치하는지 확인한다. 기존 묘사가 그대로 유효하면 재사용하고, 위장 해제·부상·장비 변경 등 플레이어에게 보이는 상태가 달라졌다면 기존 정본 안에 상황별 묘사를 보완한다.

`맵/`에서는 이전 회차나 기존 세션에서 사용할 수 있는 맵이 있으면 신규 제작보다 재사용을 우선 검토한다.

전용 스탯블록은 현재 회차에서 새로 필요할 때만 `NPC 및 몬스터/[이름].md` 통합 파일 안에 추가한다. 기존 스탯블록을 그대로 사용할 수 있으면 같은 파일의 스탯블록 정책에 이름·출처·사용 방침만 기록하고 수치나 별도 스탯블록 파일을 중복 생성하지 않는다.


`세션 프롤로그`는 이전 회차의 결말을 그대로 반복하지 않고, 이전 회차 이후 달라진 상태와 이번 회차의 시작점만 압축해 연결한다. `세션 에필로그`는 이번 회차의 결과와 다음 회차로 넘어가는 상태 변화를 정리한다. 두 장면 모두 `02. 장면 목록`과 `장면/` 폴더에 정식으로 포함한다.

장면별 파일은 해당 회차의 장면 목록이 확정되기 전에는 생성하지 않으며, 맵·스탯·이미지·토큰은 장면 기능과 묘사가 정리된 후 제작한다.
